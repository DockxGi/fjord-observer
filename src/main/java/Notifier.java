/**
 * Interface for classes that are responsible for notifying {@link Observer} objects about state changes on
 * the {@link Observable} object.
 */
public interface Notifier extends NotifiesObservers, WatchesObservable {
}
